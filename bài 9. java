gói  helloworld ;
gói  mồi bản ;
nhập  java . sử dụng . Máy quét ;
 lớp  công khai bai8 {
    public  static  void  main ( String [] args ) {
        Máy quét  sc = Máy quét mới  ( Hệ thống . In );
        Hệ thống . ra ngoài . print ( "nhap vao n:" );
        int  n = sc . nextInt ();
        int  sum = 0 ;
        int  m ;
        for ( int  i = 1 ; i <= n ; i ++) {
            Hệ thống . ra ngoài . println ( "nhap vao so nguyen:" );
            m = sc . nextInt ();
            tổng + = m ;
    
        }
       
        Hệ thống . ra ngoài . println ( "trung bình cộng:" + ( float ) ( 1.0 * sum / n ));
}
}
